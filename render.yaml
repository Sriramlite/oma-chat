services:
  - type: web
    name: oma-chat-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.9.0
      - key: MONGODB_URI
        sync: false # User must enter this manually in dashboard
